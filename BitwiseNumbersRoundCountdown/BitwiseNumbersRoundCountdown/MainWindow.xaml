<Window x:Class="BitwiseNumbersRoundCountdown.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BitwiseNumbersRoundCountdown"
        xmlns:viewModel="clr-namespace:BitwiseNumbersRoundCountdown.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Window.DataContext>
        <viewModel:Presenter/>
    </Window.DataContext>
    
    <Grid>
        <TextBlock x:Name="NumberOne" HorizontalAlignment="Left" Margin="20,20,0,0" TextWrapping="Wrap" Text="{Binding Game.NumbersList[0]}" VerticalAlignment="Top"/>
        <TextBlock x:Name="NumberTwo" HorizontalAlignment="Left" Margin="40,20,0,0" TextWrapping="Wrap" Text="{Binding Game.NumbersList[1]}" VerticalAlignment="Top"/>
        <TextBlock x:Name="NumberThree" HorizontalAlignment="Left" Margin="60,20,0,0" TextWrapping="Wrap" Text="{Binding Game.NumbersList[2]}" VerticalAlignment="Top"/>
        <TextBlock x:Name="NumberFour" HorizontalAlignment="Left" Margin="80,20,0,0" TextWrapping="Wrap" Text="{Binding Game.NumbersList[3]}" VerticalAlignment="Top"/>
        <TextBlock x:Name="NumberFive" HorizontalAlignment="Left" Margin="100,20,0,0" TextWrapping="Wrap" Text="{Binding Game.NumbersList[4]}" VerticalAlignment="Top"/>
        <TextBlock x:Name="NumberSix" HorizontalAlignment="Left" Margin="120,20,0,0" TextWrapping="Wrap" Text="{Binding Game.NumbersList[5]}" VerticalAlignment="Top"/>
    </Grid>
</Window>
